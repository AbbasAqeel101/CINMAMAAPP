name: Build iOS

on:
  push:
    branches: [ master, main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v4

    - name: Build & Archive
      run: |
        # بناء الأرشيف (هذا يجمع كل ملفات الكود والصور بشكل حقيقي)
        xcodebuild archive \
          -project CinemanaApp.xcodeproj \
          -scheme CinemanaApp \
          -sdk iphoneos \
          -configuration Release \
          -archivePath ./build/CinemanaApp.xcarchive \
          CODE_SIGNING_ALLOWED=NO \
          CODE_SIGNING_REQUIRED=NO \
          CODE_SIGNING_IDENTITY=""

    - name: Package IPA
      run: |
        # إنشاء المجلد الأساسي للتطبيق
        mkdir -p Payload
        
        # نسخ التطبيق الفعلي المترجم من داخل الأرشيف
        cp -r ./build/CinemanaApp.xcarchive/Products/Applications/*.app Payload/
        
        # ضغط المجلد لإنتاج الـ IPA
        zip -r CinemanaApp.ipa Payload
        
        # للتأكد من الحجم في سجلات GitHub (يجب أن يكون بالميغابايت)
        ls -lh CinemanaApp.ipa

    - name: Upload IPA
      uses: actions/upload-artifact@v4
      with:
        name: CinemanaApp-IPA
        path: CinemanaApp.ipa
        retention-days: 7