name: Build iOS

on:
  push:
    branches: [ master, main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v4

    - name: Build & Archive
      run: |
        # بناء الأرشيف يضمن تجميع كل ملفات المشروع في مكان واحد
        xcodebuild archive \
          -project CinemanaApp.xcodeproj \
          -scheme CinemanaApp \
          -sdk iphoneos \
          -configuration Release \
          -archivePath ./build/CinemanaApp.xcarchive \
          CODE_SIGNING_ALLOWED=NO \
          CODE_SIGNING_REQUIRED=NO \
          CODE_SIGNING_IDENTITY=""

    - name: Package IPA
      run: |
        # إنشاء المجلد المطلوب لنظام iOS
        mkdir -p Payload
        
        # نسخ الملف التنفيذي (.app) من داخل الأرشيف المترجم
        # نستخدم المسار المباشر بدلاً من البحث (find) لضمان الدقة
        cp -r ./build/CinemanaApp.xcarchive/Products/Applications/*.app Payload/
        
        # ضغط المجلد لإنتاج ملف IPA حقيقي
        zip -r CinemanaApp.ipa Payload
        
        # التحقق من الحجم في سجلات GitHub (يجب أن يظهر بالميغابايت الآن)
        ls -lh CinemanaApp.ipa

    - name: Upload IPA
      uses: actions/upload-artifact@v4
      with:
        name: CinemanaApp-IPA
        path: CinemanaApp.ipa
        retention-days: 7